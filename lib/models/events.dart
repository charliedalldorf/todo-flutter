class Events {
  int id;
  String eventName;
  String venueName;
  String city;
  String state;
  String description;
  int isActive;

  Events(this.id, this.eventName, this.venueName, this.city, this.state,
      this.description, this.isActive);
}
